cal.margin1             (Internal) Calculate the first margin for
                        random numbers, internally used for
                        parallelization
cal.margin2             (Internal) Calculate the second margin for
                        random numbers, internally used for
                        parallelization
cal.prob.AA             (Internal) Calculate probability for wild-type
                        allele, internally used for parallelization
cal.prob.AB             (Internal) Calculate probability for
                        heterozygous allele, internally used for
                        parallelization
cal.prob.BB             (Internal) Calculate probability for
                        mutant-type allele, internally used for
                        parallelization
cal.prob.categorical.data
                        (Internal) Calculate probabilities for
                        categorical data, internally used for
                        parallelization
cbind.matrix            (Internal) Combind two matrices by column for
                        big data, internally used for parallelization
create.outlier          (Internal) Create outliers,internally used for
                        parallelization
demo.filest             Demonstration the filest function
demo.filestsim          The demonstration for using the function
                        filestsim.
do.create.cate          (Internal) Create categorical data, internally
                        used for parallelization
do.sample.SNP           (Internal) Sample SNP according to given
                        probabilities, internally used for
                        parallelization
do.sample.categorical.data
                        (Internal) Sample categorical data,internally
                        used for parallelization
filest                  Simulate data for multiple populations
filestsim               A function to simulate SNP data which genetic
                        distances between populations are controlable.
generate.categorical.data
                        (Internal) Generate categorical data,
                        internally used for parallelization
generate.label          (Internal) Generate labels, internally used for
                        parallelization
generate.snp            (Internal) Generate SNPs, internally used for
                        parallelization
get.para                (Internal) Get all parameters
get.random.beta         (Internal) Get random number based on beta
                        distribution, internally used for
                        parallelization
rbind.matrix            (Internal) Combind two matrices by row for big
                        data, internally used for parallelization
save.PC.plot            (Internal) Generate PC plots labels, internally
                        used for parallelization
